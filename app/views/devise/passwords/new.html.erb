<div class="container mx-auto">
  <div class="flex justify-center px-6 my-12">
    <div class="w-full xl:w-3/4 lg:w-11/12 flex">
      <div class="w-full h-auto bg-gray-400 hidden lg:block lg:w-1/2 bg-cover rounded-l-lg" style="background-image: url('https://source.unsplash.com/oWTW-jNGl9I/600x800')"></div>
      <div class="w-full lg:w-1/2 bg-white sm:px-5 py-5 rounded-lg lg:rounded-l-none">
        <div class="sm:px-8 px-4 mb-4 text-center">
          <h3 class="pt-4 mb-2 sm:text-2xl text-md">Забыли свой пароль?</h3>
          <p class="mb-4 sm:text-sm text-xs text-gray-700">
            Мы понимаем, всякое бывает. Просто введите свой адрес электронной почты ниже, и мы вышлем вам ссылку для сброса пароля!
          </p>
        </div>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { class: 'sm:px-8 px-4 pt-6 pb-8 mb-4 bg-white rounded', method: :post }) do |f| %>
          <% if resource.errors.any? %>
            <div>
              <% resource.errors.full_messages.each do |error| %>
                <div class="my-3 block text-sm text-left text-white bg-red-500 h-12 flex items-center p-4 rounded-md" role="alert">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="w-6 h-6 mx-2 stroke-current">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                  </svg>
                  <%= error %>
                </div>
              <% end %>
            </div>
          <% end %>
          <div class="mb-4">
            <%= f.label :email, 'Почта', class: 'block mb-2 text-xs sm:text-sm font-bold text-gray-700' %>
            <%= f.email_field :email, autofocus: true, autocomplete: 'email', class: 'w-full px-3 py-2 text-xs sm:text-sm leading-tight text-gray-700 border rounded shadow appearance-none focus:outline-none focus:shadow-outline', placeholder: 'Введите адрес почты...' %>
          </div>
          <div class="mb-6 text-center">
            <%= f.submit 'Сброс пароля', class: 'relative w-full py-2 px-4 border border-transparent sm:text-sm font-bold text-xs leading-5 font-medium rounded-md text-white bg-blue-600 hover:bg-blue-500 focus:outline-none focus:border-blue-700 focus:shadow-outline-blue active:bg-blue-700 transition duration-150 ease-in-out' %>
          </div>
          <hr class="mb-6 border-t">
          <div class="text-center">
            <%= link_to 'Создайте аккаунт!', new_registration_path(resource_name), class: 'inline-block text-xs sm:text-sm text-blue-500 align-baseline hover:text-blue-800' %>
          </div>
          <div class="text-center">
            <%= link_to 'У вас уже есть аккаунт? Войдите!', new_session_path(resource_name), class: 'inline-block text-xs sm:text-sm text-blue-500 align-baseline hover:text-blue-800' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
